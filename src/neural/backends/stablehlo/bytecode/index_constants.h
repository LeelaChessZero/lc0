#pragma once

#include <cstddef>
#include <limits>

namespace lczero {
namespace stablehlo {

inline constexpr std::size_t kInvalidIndex =
    std::numeric_limits<std::size_t>::max();

}  // namespace stablehlo
}  // namespace lczero
